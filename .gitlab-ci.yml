image: monachus/hugo

variables:
  GIT_SUBMODULE_STRATEGY: recursive

pages:
  script:
  - bash scripts/generate-allbooks.sh
  - hugo

  artifacts:
    paths:
    - public
    expire_in: 1 week
  only:
  - master
  interruptible: true
